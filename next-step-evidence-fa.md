# بازبینی پس از سه جستجوی پیشوند ثابت — ۲۰۲۶-۰۹-۱۲

## نتیجه

هنوز سید یا قاعدهٔ قطعی استخراج آن پیدا نشده است. قدم انجام‌شده، بازبینی تصویر اصلی، بزرگ‌نمایی‌های موجود، کد ذخیره‌شدهٔ HomelessPhD و README عمومی آن است. نتیجهٔ قابل اتکا این است که پیشوند ده‌کلمه‌ای را نباید دادهٔ قطعی معما دانست. در این مرحله اجرای سنگین تازه‌ای شروع نشده است.

## آنچه جستجوها نشان می‌دهند

طبق گزارش‌های کاربر، پیشوند `moon tower food this real subject address total ten black` با دو جای خالی، در سه تنظیم کامل بررسی شده است:

| تنظیم | ترکیب‌ها | عبور از فیلتر | مقایسهٔ آدرس | نتیجه |
|---|---:|---:|---:|---|
| BIP39، رمز خالی | 4,194,304 | 262,144 | 10,485,760 | بدون تطابق |
| BIP39، breathe و BREATHE | 4,194,304 | 262,144 | 20,971,520 | بدون تطابق |
| Electrum استاندارد، رمز خالی و breathe | 4,194,304 | 16,470 | 1,317,600 | بدون تطابق |

این سه اجرا سه مجموعهٔ مستقل کلمات نبودند؛ همگی به همان ده کلمه و همان ترتیب وابسته بودند. شکست آن‌ها مشخص نمی‌کند کدام واژه غلط است. کلیدهای دیگر حاصل از مسیرها یا رمزهای خارج از تنظیمات نیز رد نشده‌اند.

## شواهد در برابر برداشت

| نامزد یا ادعا | مشاهده | تصمیم |
|---|---|---|
| moon و tower | روی عقربه‌ها خوانده می‌شوند | حفظ به‌عنوان نامزدهای قوی، بدون تثبیت جایگاه ۱ و ۲ |
| subject | زیرخط در پایهٔ مجسمه دیده می‌شود | حفظ به‌عنوان نامزد قوی، بدون تثبیت جایگاه ۶ |
| food | راهنمای اولیه و تصویر برج؛ محل بررسی، ساقهٔ برج است | حفظ نامزد؛ از تصویر کوچکِ سر برج نتیجه نگیریم |
| this، real، black | حضور یا تکرار در نوشته‌ها | حفظ نامزد؛ حضور، عضویت و ترتیب در سید سه ادعای متفاوت‌اند |
| address، total، ten | در خط ۹۱ کد ذخیره‌شده جزو پیشوند هستند | برای عضویت و جایگاهشان در تصویر شاهد مستقل کافی پیدا نکردم |
| black در جایگاه ۱۰ | نیازمند خواندن نماد انتهایی به‌صورت X، تعبیر آن به ۱۰ و نسبت دادن عدد به black است | یک فرض شرطی با چند مرحلهٔ اثبات‌نشده |

وجود آدرس بیت‌کوین در حاشیه به‌تنهایی کلمهٔ address را تعیین نمی‌کند. تبدیل «جمع» به total یا نماد به ten هم استخراج مستقیم حروف نیست. این‌ها برداشت‌های ممکن‌اند، نه سرنخ‌های تأییدشده.

## شاخهٔ استنتاجی منسجم: جمع عددهای مجاور عقربه

با پذیرش ترجمهٔ ارائه‌شدهٔ «جمع دو عدد»، یک تفسیر قابل بررسی این است که دو عدد مجاور هر عقربه، جایگاه واژهٔ روی آن را بدهند:

- tower میان ۱ و ۲: جایگاه ۳.
- moon میان ۱۲ و ۱: جایگاه ۱۳.

در این شاخه، عبارت حداقل ۱۳ واژه دارد؛ بنابراین جستجوی ۱۲کلمه‌ای مناسب نیست. این مشاهده طول دقیق را تعیین نمی‌کند. برای عقربهٔ سوم، جمع ۱۰ و ۱۱ برابر ۲۱ است، اما واژهٔ مربوط به آن را مستقل نخوانده‌ایم؛ پس جایگاه ۲۱ را قید قطعی نمی‌کنیم.

این برداشت قبلاً در یادداشت‌ها و جدول عمومی HomelessPhD مطرح بوده است؛ کشف تازه یا اثبات حل نیست. اهمیت فعلی آن نشان دادن ناسازگاری پیشوند آزمایش‌شده با این قاعده است. همچنین جمع‌های همسایه می‌توانند تکراری باشند؛ مثلاً ۱۲+۱ و ۶+۷ هر دو ۱۳ می‌دهند. پس قاعده هنوز برای ترتیب یکتای کل عبارت کافی نیست.

## تصمیم اجرایی

پیشوند قبلی مبنای گسترش خودکار نخواهد بود. شاخهٔ ساعت را به‌عنوان فرضیهٔ شرطی نگه می‌داریم، اما از دو جایگاه به یک سید بلندِ قابل جستجو نرسیده‌ایم. برای ساخت جستجوی بعدی باید یک قاعده بتواند چند سرنخ مستقل دیگر را نیز بدون انتخاب دلخواه توضیح دهد؛ مثلاً رابطهٔ عددها با متن زیرخط‌دار مجسمه یا نوشته‌های ریز عنوان مرکزی.

فعلاً فایل START جدیدی پیشنهاد نمی‌شود؛ جایگزین کردن چند حدس دلخواه فقط دامنهٔ متفاوتی می‌سازد و شواهد را تقویت نمی‌کند. اجرای دوبارهٔ تنظیمات تمام‌شده نیز اطلاعات جدیدی نمی‌دهد.

## منابع بررسی

- تصویر محلی: `0.2-btc-puzzle.png`
- بزرگ‌نمایی‌ها: `clue-inspection/clock.png`، `clue-inspection/statue_text.png` و `clue-inspection/right-rune-end.png`
- پیشوند در کد محلی: `homelessphd-review/source.py.txt`، خط ۹۱
- جدول فرضیات عمومی: https://github.com/HomelessPhD/BLM_0.2BTC#table-of-ideas
- کد عمومی: https://github.com/HomelessPhD/BLM_0.2BTC/blob/main/python_script/BLM_generate_BIP39_pk.py

جدول عمومی خودش احتمال نادرست بودن واژه‌ها را تصریح می‌کند. آن را مرجع فرضیه‌ها تلقی کردیم، نه پاسخ تأییدشدهٔ سازنده.
