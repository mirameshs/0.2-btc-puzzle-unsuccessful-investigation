# راهنمای برنامهٔ جست‌وجوی معمای بیت‌کوین

این برنامه فقط به‌صورت آفلاین عبارت‌های احتمالی همین معمای عمومی را تولید و با آدرس `1KfZGvwZxsvSmemoCmEV75uqcNzYBHjkHZ` مقایسه می‌کند. پاسخ هنوز پیدا نشده و واژه‌های تنظیمات، فرضیه‌اند. برنامه عملیات برداشت یا انتقال ندارد و به اینترنت یا موجودی‌گیری از کیف پول‌های دیگر متصل نمی‌شود.

## اجرای ساده روی همین لپ‌تاپ

روی `START-SOLVER.cmd` دوبار کلیک کنید. پنجره‌ای باز می‌شود و جست‌وجوی پیش‌فرض شروع یا ادامه پیدا می‌کند. راه‌انداز ابتدا Python موجود در محیط Codex این دستگاه را پیدا می‌کند؛ اگر آن مسیر موجود نباشد، `py` و سپس `python` را امتحان می‌کند.

برای توقف، داخل همان پنجره `Ctrl+C` بزنید و صبر کنید پیام `paused` نمایش داده شود. با اجرای دوباره، ادامهٔ جست‌وجو انجام می‌شود. هنگام این تحویل فقط یک آزمایش کوتاه اجرا شده؛ جست‌وجوی طولانی در پس‌زمینه روشن نمانده است.

## اجرا با Python معمولی روی دستگاه دیگر

Python نسخهٔ ۳٫۱۰ یا جدیدتر لازم است. فایل برنامه، فایل واژه‌های رسمی و JSON تنظیمات را در یک پوشه نگه دارید. برای تنظیم ۱۰۲واژه‌ای، فایل `seed-candidates.txt` هم لازم است.

در ترمینالِ همان پوشه:

```powershell
py -3 -m pip install -r requirements-solver.txt
py -3 puzzle_solver.py
```

اگر دستور `py` موجود نیست، به‌جای `py -3` از `python` استفاده کنید. نصب وابستگی به اینترنت نیاز دارد؛ خود جست‌وجو ندارد.

## تنظیمات آماده

| فایل | فرضیه و دامنه |
|---|---|
| `puzzle-search.json` | ۱۲ واژهٔ منتخب، `black` در جایگاه دهم، تمام ۳۹٬۹۱۶٬۸۰۰ ترتیب باقی‌مانده؛ پیش‌فرض |
| `puzzle-search-all-orders.json` | همان ۱۲ واژه بدون جایگاه ثابت؛ تمام ۴۷۹٬۰۰۱٬۶۰۰ ترتیب |
| `puzzle-search-slots.json` | جایگزین‌های معنایی راهنمایی‌ها، چرخش‌ها و ترتیب معکوس؛ ۱۳٬۸۲۴ حالت؛ بخشی از این دامنه قبلاً بی‌نتیجه بررسی شده است |
| `puzzle-search-102-words.json` | انتخاب مرتب ۱۲ واژه از فایل ۱۰۲تایی؛ دامنهٔ بسیار بزرگ و نامناسب برای جست‌وجوی کامل روی لپ‌تاپ |

واژه‌های پیش‌فرض:

```text
moon tower food this subject real black one order stock life vote
```

این فهرست تمام نامزدها را پوشش نمی‌دهد. خصوصاً `one/order/stock/life/vote` انتخاب‌های آزمایشی برای کامل‌کردن مجموعه‌اند و جایگاه دهم `black` نیز قطعی نیست. پایان بی‌نتیجه فقط همین تنظیم را رد می‌کند.

برای انتخاب تنظیم دیگر:

```powershell
py -3 puzzle_solver.py --config puzzle-search-all-orders.json
```

هر فایل تنظیمات به‌طور پیش‌فرض فایل پیشرفت جداگانه دارد. در ترمینال PowerShell می‌توانید راه‌انداز را نیز با آرگومان اجرا کنید:

```powershell
.\START-SOLVER.cmd --config puzzle-search-slots.json
```

## مصرف پردازنده و آزمایش کوتاه

برای استفاده از همهٔ پردازنده‌های منطقی، روی `START-SOLVER-FULL-CPU.cmd` دوبار کلیک کنید. روی این لپ‌تاپ ۲۴ پردازش فعال می‌شود و استراحت عمدی بین دسته‌ها حذف می‌شود. ابتدا اجرای قبلی را با `Ctrl+C` متوقف کنید و منتظر پیام `paused` بمانید؛ راه‌انداز جدید همان پیشرفت را ادامه می‌دهد.

دستور معادل:

```powershell
py -3 puzzle_solver.py --full-cpu
```

این گزینه بر `--workers` و `--duty` اولویت دارد. استفادهٔ دقیق و دائمی ۱۰۰٪ تضمین نمی‌شود؛ زمان‌بندی سیستم‌عامل، فعالیت برنامه‌های دیگر و محدودیت توان پردازنده مؤثرند. این حالت تنظیم برق یا اولویت پردازش ویندوز را تغییر نمی‌دهد. در ویندوز تعداد پردازش‌ها حداکثر ۶۱ است، مطابق محدودیت ProcessPoolExecutor.

پیش‌فرض دو پردازش و سهم فعالیت ۶۵٪ برای هر پردازش است. `duty` سقف دقیق مصرف کل CPU نیست؛ برنامه بین دسته‌ها استراحت می‌کند. برای کار سبک‌تر:

```powershell
py -3 puzzle_solver.py --workers 1 --duty 0.5
```

برای استفادهٔ بیشتر از پردازنده، تعداد پردازش‌ها را متناسب با لپ‌تاپ افزایش دهید:

```powershell
py -3 puzzle_solver.py --workers 4 --duty 0.9
```

آزمایش تقریباً یک‌دقیقه‌ای:

```powershell
py -3 puzzle_solver.py --max-seconds 60
```

بعد از این زمان، ارسال کار جدید متوقف می‌شود؛ دسته‌های در حال اجرا تمام می‌شوند، پس توقف دقیقاً سر ثانیهٔ ۶۰ نیست. اندازهٔ پیش‌فرض دسته ۶۴ حالت است و حداکثر دو دسته به‌ازای هر پردازش در صف قرار می‌گیرد. برای واکنش سریع‌تر به توقف از `--batch-size 16` استفاده کنید.

برای اجرای فقط ۱۰هزار حالتِ اضافه:

```powershell
py -3 puzzle_solver.py --max-candidates 10000
```

## معنی خروجی و تخمین زمان

- `cases/s`: تعداد حالت‌های ترتیب در ثانیه، شامل موارد ردشده توسط checksum.
- `valid`: تعداد عبارت‌های دارای checksum درست؛ به معنی پیدا شدن جایزه نیست.
- `checks`: تعداد مقایسه‌های آدرس با هدف.
- `ETA`: تخمین پایان همین دامنه با سرعت فعلی؛ وعدهٔ زمان پیدا شدن پاسخ نیست.
- `exhausted`: همهٔ حالت‌های همین تنظیم بررسی شده و تطبیقی پیدا نشده است.
- `paused`: پیشرفت ذخیره شده و اجرای مجدد ادامه می‌دهد.

جست‌وجوی ۱۰۲واژه‌ای ۶۴۷٬۱۲۶٬۸۳۶٬۱۴۳٬۸۶۴٬۶۲۰٬۵۴۴٬۰۰۰ حالت بدون تکرار دارد. حتی داشتن برنامهٔ بروت‌فورس این دامنه را عملی نمی‌کند؛ محدودکردن واژه‌ها و ترتیب از سرنخ‌ها لازم است.

## ذخیره و ادامه

پیشرفت در `solver-output/<نام تنظیم>.state.json` ذخیره می‌شود. برنامه هر حدود ۱۰ ثانیه و هنگام خروج عادی، پیشرفتِ دسته‌های کامل‌شدهٔ متوالی را ذخیره می‌کند. نوشتن با فایل موقت و جایگزینی اتمی انجام می‌شود. بعد از قطع برق یا بستن ناگهانی، بخشی از کار آخر ممکن است دوباره انجام شود، اما برنامه از روی قسمت ذخیره‌نشده نمی‌پرد.

با تغییر کلمات، ترتیب فهرست، رمز تکمیلی یا مسیرها، فضای جست‌وجو عوض می‌شود. برنامه عمداً از ادامه با پیشرفت قبلی خودداری می‌کند. تنظیم جدید را با نام تازه ذخیره کنید یا فایل وضعیت دیگری بدهید:

```powershell
py -3 puzzle_solver.py --config puzzle-search.json --state solver-output/experiment-2.json
```

تغییر تعداد پردازش‌ها، `duty` یا اندازهٔ دسته به فایل وضعیت جدید نیاز ندارد. قفل سیستم‌عامل مانع اجرای هم‌زمان دو برنامه روی یک فایل پیشرفت می‌شود؛ بعد از قطع ناگهانی، قفل آزاد می‌شود.

راه دیگر توقف: فایل خالی با مسیر `Stop file` که برنامه نمایش می‌دهد بسازید. برای تنظیم پیش‌فرض، مسیر آن `solver-output/puzzle-search.state.stop` است. بعد از توقف، برای ادامه آن فایل را حذف کنید. وجود فایل قفل به‌تنهایی نشانهٔ درحال‌اجرا بودن برنامه نیست.

زمانی که لپ‌تاپ Sleep یا Hibernate باشد محاسبه جلو نمی‌رود. برای اجرای مداوم، تنظیم Sleep دستگاه باید متناسب با خواستهٔ شما باشد؛ برنامه تنظیمات برق ویندوز را تغییر نمی‌دهد.

## اگر تطبیق واقعی پیدا شود

برنامه فایل `solver-output/matches/MATCH-....json` می‌سازد. فایل شامل عبارت بازیابی، رمز تکمیلی، مسیر، نوع کلید عمومی و کلید خصوصی همان آدرس به‌صورت HEX و WIF است. موفقیت فقط زمانی ثبت می‌شود که آدرس به‌طور دقیق با آدرس جایزه برابر باشد. کارگر نتیجه را بلافاصله ذخیره می‌کند و سپس جست‌وجو متوقف می‌شود؛ برخی دسته‌های ازقبلشروع‌شده ممکن است تا پایان ادامه یابند.

این خروجی‌ها متن ساده‌اند. checksum معتبر یا پیدا شدن آدرسی متفاوت، موفقیت ثبت نمی‌شود.

## ساخت فرضیهٔ شخصی

در حالت `permutations`، از `pool` یا `pool_file` استفاده کنید. هرکدام از واژه‌ها حداکثر یک بار در عبارت به کار می‌روند. برای ۱۲ واژه، `word_count` برابر ۱۲ است. جایگاه‌ها از ۱ شروع می‌شوند؛ برای مثال:

```json
"fixed_positions": {"3": "tower", "10": "black"}
```

کلمات ثابت باید در فهرست نامزدها وجود داشته باشند. در حالت `slots` برای هر جایگاه یک فهرست بدهید؛ تکرار یک واژه در جایگاه‌های متفاوت مجاز است. طول‌های ۱۲، ۱۵، ۱۸، ۲۱ و ۲۴ پشتیبانی می‌شوند، ولی فایل‌های آماده فقط فرضیه‌های ۱۲کلمه‌ای هستند. فعال‌کردن چرخش و معکوس برای بعضی جایگاه‌ها ممکن است حالت‌های تکراری بسازد؛ حافظه برای نگهداری مجموعهٔ تمام حالت‌ها مصرف نمی‌شود.

تنظیم `passphrases` شامل رمزهای تکمیلی جدا از کلمات سید است؛ `""` یعنی بدون رمز تکمیلی. نمونه‌های آماده فقط خالی، `breathe` و `BREATHE` را آزمایش می‌کنند و جست‌وجوی تمام رمزهای ممکن انجام نمی‌دهند.

`base_paths` مسیرهای پایه‌اند؛ شاخص‌های تعیین‌شده توسط `index_start` و `index_count` به انتهای آن‌ها اضافه می‌شوند. تنظیم پیش‌فرض شاخص‌های ۰ تا ۴ از چهار مسیر و کلید اصلی را بررسی می‌کند. فقط آدرس mainnet P2PKH مطابق هدف بررسی می‌شود. الگوریتم کیف پول قدیمی Electrum، عبارت‌های غیر-BIP39 و تمام مسیرهای ممکن پشتیبانی نمی‌شوند.

`compressed_modes` با `[true]` کلید عمومی فشرده و با `[true, false]` هر دو نوع را آزمایش می‌کند؛ اضافه‌کردن موارد، زمان بررسی را بیشتر می‌کند.

برای دیدن تعداد حالت‌ها بدون شروع جست‌وجو:

```powershell
py -3 puzzle_solver.py --config puzzle-search-102-words.json --describe
```

## راستی‌آزمایی برنامه

آزمون رمزنگاری کوتاه در شروع هر اجرا انجام می‌شود. همچنین:

```powershell
py -3 puzzle_solver.py --self-test
py -3 -m unittest -v test_puzzle_solver.py
```

آزمون کامل به فایل موجود `bip39-test-vectors.json` نیاز دارد. آزمون‌ها شامل بردارهای مرجع، تطبیق آدرس معلوم، ثبت نتیجه، شمارش جایگشت، ادامهٔ چندپردازشی بدون جاانداختن حالت و تشخیص تغییر تنظیمات است. آزمون آدرس معلوم از عبارت عمومی آزمایشی استفاده می‌کند و حل این معما محسوب نمی‌شود.
